# GitHub PAT (fine-grained, scope: public_repo, read-only).
# Optional but recommended: lifts rate limit from 60 -> 5,000 req/hr and enables code search.
GITHUB_TOKEN=

# Optional: HTTP port the server listens on. Defaults to 9247.
# PORT=9247
